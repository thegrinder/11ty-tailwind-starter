---
layout: layouts/document.njk
---

<body class="bg-slate-100 text-neutral-700 dark:bg-slate-900  dark:text-neutral-100 font-body h-full relative">
  {% include "partials/intro.njk" %}
  <nav class="container mx-auto relative z-10">
    <div class="h-20 flex w-full justify-between">
      <ul class="h-full flex gap-2 text-xl">
        <li class="h-full">
          <a id="home" class="h-full flex items-center px-4 font-semibold" href="/">P.</a>
        </li>
        <li class="h-full">
          <a id="work" class="h-full flex items-center px-4 font-semibold" href="/work">Work</a>
        </li>
        <li class="h-full">
          <a id="contact" class="h-full flex items-center px-4 font-semibold" href="/contact">Contact</a>
        </li>
      </ul>
      <button aria-label="Toggle color mode" id="color-mode-button" class="h-full flex items-center px-4 text-lg">
        {% include "icons/light-bulb.njk" %}
      </button>
    </div>
  </nav>
  <main class="container mx-auto px-4 relative -top-16">
    {{ content | safe }}
  </main>
</body>