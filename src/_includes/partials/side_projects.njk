{%- from "components/work/macro.njk" import work -%}
<section class="py-24">
  <h2 class="text-neutral-800 dark:text-white text-4xl sm:text-6xl font-semibold mb-24">Side projects</h2>
  <div class="grid grid-cols-1 lg:grid-cols-2 gap-14">
    {% for project in side_projects %}
      {{ work(project) }}
    {% endfor %}
  </div>
</section>