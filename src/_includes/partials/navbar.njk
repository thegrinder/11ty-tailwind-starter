<nav class="container mx-auto px-2 sm:px-4 relative z-10">
  <div class="h-20 flex w-full justify-between">
    <ul class="h-full flex gap-2 text-xl">
      {% for nav_link in nav_links %}
        <li class="h-full">
          <a id="{{ nav_link.id }}" class="custom-ring ring-inset rounded-md h-full flex items-center px-4 font-semibold" href="{{ nav_link.href }}">
            {{ nav_link.label }}
          </a>
        </li>
      {% endfor %}
    </ul>
    <button id="color-mode-button" class="ring-inset custom-ring rounded-md flex items-center px-4 text-lg">
      {% include "icons/light_bulb.njk" %}
    </button>
  </div>
</nav>