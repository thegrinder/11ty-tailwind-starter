<footer class="container mx-auto px-4 sm:px-8 relative text-center p-8">
  <ul class="flex items-center justify-center gap-1 mb-2">
    {% for social_link in social_links %}
      <li>
        <a class="p-1.5 inline-block custom-ring rounded-md" aria-label="{{ social_link.label }}" target="_blank" href="{{ social_link.href }}">
          {% include "icons/" + social_link.icon + ".njk" %}
        </a>
      </li>
    {% endfor %}
  </ul>
  <p class="text-slate-400 dark:text-slate-500">©Szczepkowski 2022</p>
</footer>